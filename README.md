# aprendendocomodjango

# Aprendendo com Django

Este repositório contém os projetos e códigos desenvolvidos durante o processo de aprendizado do framework web [Django](https://www.djangoproject.com/) e do [Django REST Framework](https://www.django-rest-framework.org/).

## 🚀 Sobre o Projeto

O objetivo principal deste repositório é servir como um diário de bordo e portfólio dos estudos em Django. Aqui são armazenados exemplos, pequenos projetos e testes de funcionalidades para solidificar o conhecimento no framework, incluindo a criação de APIs RESTful.

## 🛠️ Tecnologias Utilizadas

* **Python:** Linguagem de programação base.
* **Django:** O framework web principal.
* **Django REST Framework (DRF):** Toolkit para construir APIs Web.
* **HTML:** Para a estruturação das páginas (quando aplicável).

## 📋 Requisitos

Para executar este projeto, você precisará ter os seguintes softwares instalados:

* [Python 3.x](https://www.python.org/downloads/)
* [Pip](https://pip.pypa.io/en/stable/installation/) (geralmente vem com o Python)
* [Git](https://git-scm.com/downloads) (para clonar o repositório)

### Dependências do Projeto

As dependências específicas do Python para este projeto são:

* **Django**
* **djangorestframework**

> **⚠️ Atenção:** O projeto não possui um arquivo `requirements.txt`. É uma boa prática adicionar um para facilitar a instalação das dependências por outros desenvolvedores (ou por você mesmo no futuro).
>
> Você pode gerar um facilmente após instalar suas dependências com o comando:
> `pip freeze > requirements.txt`

## ⚙️ Como Executar o Projeto

Siga os passos abaixo para rodar o projeto localmente:

1.  **Clone o repositório:**
    ```bash
    git clone [https://github.com/VitorHugoTeles/aprendendocomodjango.git](https://github.com/VitorHugoTeles/aprendendocomodjango.git)
    ```

2.  **Acesse o diretório do projeto:**
    ```bash
    cd aprendendocomodjango
    ```

3.  **(Recomendado) Crie e ative um ambiente virtual:**
    ```bash
    # Criar o ambiente
    python -m venv venv
    
    # Ativar no Windows
    .\venv\Scripts\activate
    
    # Ativar no Linux/Mac
    source venv/bin/activate
    ```

4.  **Instale as dependências:**
    Como não há `requirements.txt`, você precisará instalar as dependências manualmente:
    ```bash
    pip install Django
    pip install djangorestframework
    ```

5.  **Acesse o diretório
